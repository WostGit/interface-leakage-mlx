[project]
name = "interface-leakage-mlx"
version = "0.1.0"
description = "Minimal reproducible experiments for extraction under different model APIs"
requires-python = ">=3.10"
dependencies = [
  "numpy==2.1.3",
  "pandas==2.2.3",
  "matplotlib==3.9.2",
]

[tool.ruff]
line-length = 100
